# GoldScalpTrader — Project Build and Recovery Guide

**Status:** POST-AUDIT-1 AGENT OPERATING MANUAL — IMPLEMENTATION NOT STARTED
**Version:** 1.0-fresh-zero-build-recovery
**Authority:** AI/developer build sequence, documentation-first implementation, context recovery, safe continuation and completion classification.

## 1. Mission / current stage

Work from canonical `Documents/` as durable project truth.

```text
64-file manual              COMPLETE
Fresh-Zero Audit 1          COMPLETE
Affected-graph sync         being verified in current packet
Implementation              NOT STARTED
```

Do not guess missing behaviour from nearby code or call green tests project completion.

## 2. Agent loop

```text
ORIENT → SCOPE → TRACE → CONTRACT → IMPLEMENT → VERIFY → SYNC → HANDOFF
```

Before coding identify purpose, owner, typed/fresh inputs, outputs/state, logical independent work vs ordered authority, fail-closed cases, persistence/recovery, operator view, research and proof boundaries.

## 3. Canonical recovery order after context loss

```text
inspect current main HEAD
→ Documents/README + Documentation Standard/Preservation
→ System Contract + Architecture
→ relevant topic + active Decisions/Open Questions
→ Module Structure + File/Test Catalog
→ current source/tests/diff/evidence
→ continue first incomplete dependency
```

Repository truth beats remembered chat.

## 4. Frozen architecture shortcuts for orientation

- H1 soft regime; M15 Opportunity; M5 production setup/timing/management; H4 optional; M1 diagnostic/research only.
- six frozen strategy families with correlation control.
- TradePlan carries gross + cost-adjusted room before one STANDARD Risk policy.
- News UNKNOWN blocks new entry; management remains action-sensitive.
- READINESS/DRY_RUN first; controlled DEMO writer later; REAL outside V1.
- Trade Manager uses HOLD/PROTECT/TRAIL/RUNNER/EXIT; time weakness is EXIT reason.
- logical parallel analysis; physical concurrency optional; financial/broker authority serial.
- no trading-runtime Git operations.

## 5. Implementation packet

Every material feature packet includes owning contract, source owner, typed inputs/outputs, chronology/freshness, failure/UNKNOWN behaviour, persistence/restart effect, tests, operator mapping, research effect, evidence boundary and full affected documentation graph.

## 6. Docs/code/test disagreement

- frozen contract correct/code wrong → fix code + regression;
- accepted behaviour newer/docs stale → repair authority + graph;
- source/test map stale → correct catalog;
- true design conflict → stop and classify in Decisions/Open Questions;
- reference differs → current scalp Documents win;
- safer code exposes stale docs → update docs, do not weaken safety.

## 7. Test failure loop

```text
reproduce
→ identify invariant/owner
→ root-cause fix + regression
→ focused proof
→ module/integration proof
→ full local verification
→ affected graph sync
```

Never replace UNKNOWN with optimistic defaults to make tests pass.

## 8. Broker-write crash rule

If later Intent is `SUBMITTING`/ack ambiguous:

```text
DO NOT RESEND
→ restore Intent identity
→ query positions/orders/deals
→ reconcile exact lifecycle
→ only then allow another governed Intent
```

Known ManagedTrade disappearance requires exact known-ticket/full-volume exit proof before clearing/learning.

## 9. Corrupt/missing runtime state

Block affected writes, preserve diagnostics, restore last verified checkpoint to NEW DB/path, connect intended MT5 scope, reconcile broker/lifecycle/risk/learning truth and remain RECONCILING while critical facts are unresolved.

Never delete state and invent zero exposure/risk.

## 10. Runtime backup / machine recovery

Primary runtime recovery authority is a verified local full checkpoint/recovery package, not GitHub.

```text
safe old PRIMARY stop
→ controller/MT5 authority released
→ final local checkpoint/package
→ deliberate transfer
→ restore NEW DB/path
→ configure credentials separately
→ current broker positions/deals/quote/identity
→ reconcile durable lifecycle
→ acquire new controller
→ READY only after hard authorities pass
```

Same-scope simultaneous writers are unsupported.

## 11. Development/source backup

Normal operator-preferred low-usage workflow:

```text
one coherent remote bulk commit
→ user git pull --ff-only
→ local clone now contains latest files + full Git history
→ optional secret-clean local ZIP milestone copy
```

Do not ask for a pull after every micro-patch. Git bundle is optional advanced/manual only.

Runtime source publication is explicitly absent: no shutdown add/commit/push/pull and no runtime GitHub credentials.

## 12. Audit/evidence order

Audit 1 is complete architecture evidence. Audit 2 later checks Documents→Code→Tests. Corrective Audits 3–7 run only when implementation/connected evidence triggers their scope.

A phase is complete only when source exists, focused/full proof actually passes, failure/restart paths are explicit, source/test/catalog current, affected Documents synchronized, secret/manual gates pass and evidence classes remain honest.

## 13. Next safe dependency

After final contradiction scan of this post-Audit-1 packet, implementation begins with package/config/domain/read-only MT5 market truth. Broker writer code is **not** the first implementation step.