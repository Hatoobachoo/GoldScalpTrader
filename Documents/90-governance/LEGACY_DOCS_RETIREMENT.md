# GoldScalpTrader — Legacy Docs Retirement

**Status:** FROZEN GOVERNANCE RECORD — NO PARALLEL LEGACY TREE EXISTS
**Version:** 1.0-single-canonical-authority
**Authority:** Canonical record preventing creation of a second competing documentation tree.

## 1. Decision

GoldScalpTrader currently has no historical lowercase `docs/` product manual that needs migration/retirement.

```text
Documents/       = only current documentation authority
lowercase docs/  = must not become a competing active manual
Git history       = historical source for prior revisions
```

## 2. Why this file exists

The project preserves the complete reference-equivalent documentation topology. This record also prevents a future contributor from recreating a parallel manual by habit.

It records truthful repository state; it does not fabricate a retirement event that never occurred.

## 3. If legacy material appears later

```text
inventory legacy material
→ compare with canonical Documents/
→ preserve useful rationale/features/defaults
→ classify any proposed difference under preservation-first rule
→ record relocation/supersession
→ update Preservation Ledger / Comparison / Coverage Matrix
→ keep exactly one active documentation authority
```

Do not delete or replace useful inherited material merely for simplification.

## 4. GoldSwingTraderAI reference relationship

GoldSwingTraderAI `Documents/` is an external design/preservation baseline, not a second active tree inside this repository.

Current rule:

> Preserve its feature/default unless a direct scalp requirement, explicit operator instruction or separately proven reference defect justifies a governed difference.

Current GoldScalpTrader topic contracts remain runtime/build authority; the exact reference deltas live in `DOCUMENTATION_COMPARISON.md` and preservation status in `PRESERVATION_LEDGER.md`.

## 5. Prohibited state

Do not maintain contradictory active truth such as:

```text
Documents/ with rule A
+ docs/ with rule B
+ top-level manual/prompt with rule C
```

Top-level README/build prompts summarize canonical topic contracts and may not override them.

## 6. Current state

```text
RETIREMENT ACTION: NOT APPLICABLE
PARALLEL LEGACY TREE: ABSENT
CANONICAL AUTHORITY: Documents/
REFERENCE BASELINE: GoldSwingTraderAI under preservation-first rule
```

If topology changes, update this record in the same affected-graph packet.