# GoldScalpTrader — Final Release Audit

**Status:** FROZEN AUDIT TEMPLATE — NOT RUN
**Version:** 1.0-preservation-first-final-release
**Authority:** Evidence-backed release decision for one exact build, configuration, dataset, terminal and environment.

## 1. Purpose and scope

This is an evidence record, not a permanent completion claim.

Initial release target is local Windows + Exness MT5, documentation-first, READINESS/DRY_RUN before controlled DEMO broker-write milestone, one active PRIMARY per account/symbol scope and local-only runtime backup/recovery.

Future REAL is a preserved governed capability but is **not** authorized by a DEMO release audit. It requires a separate future release/explicit-approval gate.

This file never claims profitability.

## 2. Audit identity

```text
Audit ID:
Git revision:
Audit timestamp UTC:
Release owner:
Target broker/account/server/symbol (redacted):
Capability stage:
OS / Python / dependencies:
Configuration fingerprint:
Policy/schema versions:
Evidence package fingerprint:
Local backup/recovery package:
```

## 3. Allowed statuses

```text
PASS
FAIL
BLOCKED
NOT RUN
EXTERNAL PROOF PENDING
SCALP CALIBRATION PENDING
NOT APPLICABLE
```

Every PASS identifies concrete evidence.

## 4. Canonical Documents audit

Verify:

- complete 64-file canonical tree;
- Status/Version/Authority current;
- preservation-first rule consistent;
- no contradiction among System Contract/topic contracts/Architecture/Coder Guide/Module/Catalog;
- affected-graph synchronization;
- Audit 1 + preservation correction complete;
- remaining genuine scalp questions classified;
- frozen timeframe authority not described as unresolved;
- preserved Risk/session/provider defaults not described as generic calibration;
- bounded analytical concurrency preserved;
- future REAL described as gated capability;
- no runtime Git publication requirement.

## 5. Source/module audit

Traverse every source group from `MODULE_STRUCTURE.md` and `FILE_AND_TEST_CATALOG.md`.

For each material component record purpose, canonical owner, preservation/delta classification where relevant, files, tests, negative/failure semantics, persistence/recovery effect, operator/research effect, external/calibration evidence and verdict.

Aggregate test success cannot substitute for traversal.

## 6. Chronology / intelligence audit

Verify:

- completed-candle authority / knowledge timestamps;
- no future-confirmed swing/event leakage;
- frozen roles H1 broad / M15 opportunity / M5 production / H4 optional / M1 diagnostic;
- snapshot freshness/quality;
- bounded-parallel ↔ one-worker semantic parity;
- optional confluence remains bounded/optional;
- event freshness traceable;
- consumed/stale geometry not reused incorrectly.

## 7. Strategy / TradePlan / Risk audit

Verify:

- six preserved families;
- independent BUY/SELL + Red Team;
- persistent Opportunity/timing separation;
- fresh-event re-arm;
- one genuinely fresh same-episode re-entry baseline;
- family-specific invalidation only from proven geometry;
- gross + cost-adjusted room;
- no automatic inherited Swing 1.20R hard scalp floor;
- immutable original R;
- dynamic/min-lot sizing;
- fixed UTC-day SMALL/MEDIUM/NORMAL profile resolution;
- canonical target/elevated/hard/daily profile bands;
- aggressive mode disabled by default;
- if enabled, 8% max SL-risk ceiling **not target**, 16% aggregate/daily ceilings;
- manual daily-loss reset default disabled;
- three-loss / at-least-30-minute cooldown + release conditions;
- no stop distortion to fit account.

## 8. Session / News audit

Verify:

- current Exness schedule externally established;
- provider TTL baseline 1800s implemented/versioned;
- Daily PRE_CLOSE T-20/T-10;
- Weekend PRE_CLOSE T-60/T-30;
- daily reopen 1 clean M5;
- weekend reopen 2 clean M5 + gap assessment;
- true News UNKNOWN new-entry block/limited;
- valid LKG cache survival after refresh failure;
- no timestamp/TTL laundering;
- expired/invalid/no cache → UNKNOWN;
- known blackout hard new-entry block;
- holiday/schedule ambiguity fail-safe.

## 9. Execution / lifecycle audit

Where DEMO broker-write capability exists, verify intended capability/account/server/symbol; Gate composition; fresh spread/drift/trigger-age; one-shot Intent/persist-before-send; sole writer; ambiguous ack reconciliation; controller fencing; foreign exposure non-adoption; exact close proof; action-sensitive MODIFY/CLOSE; broker-valid partial management where applicable; restart no-duplicate semantics.

Before implementation these remain NOT RUN/NOT APPLICABLE, never fake PASS.

Also verify no hidden config/UI path activates future REAL.

## 10. Management / learning audit

Verify HOLD/PROTECT/TRAIL/RUNNER/EXIT, no stop widening, time-efficiency semantics, optional partial-management safety, queue/receipt ordering, frozen family/policy/Risk-profile identity, causal path metrics, exactly-once MAIN_DEMO learning and close-origin attribution.

Research cannot acquire broker authority.

## 11. Research audit

Verify production-semantics replay, frozen timeframe parity, preserved Risk/session policy parity, one-position capacity, no-lookahead, explicit costs/latency, min-lot classification, walk-forward, one-shot holdout, stress, Shadow/DEMO Canary, preservation classification and self-promotion denial.

No backtest result is release permission.

## 12. Operator audit

Verify terminal/browser read-only authority, profile/overlay truth, 8%-ceiling wording, blocker-vs-Gate truth, no fabricated geometry/performance, News/provider/cache truth, width/fallback behavior, UI crash isolation and verified performance provenance.

## 13. Local backup / recovery audit

Verify consistent StateStore backup, complete namespaces, manifest/hashes/catalog, secret exclusion, root outside repo, final checkpoint after authority release, no runtime Git operation, development sync workflow, restore to new DB/path, fresh broker reconciliation/controller and sequential same-scope handoff.

## 14. Connected DEMO evidence

When applicable retain exact evidence for READINESS identity/data, natural qualified setup, OPEN, MODIFY/SL/TP, CLOSE, broker-side SL/TP, known manual-close recovery, actual learning, restart/reconcile, pre-close/reopen and local backup/independent restore.

No policy is weakened to manufacture evidence.

## 15. Genuine calibration evidence

Numerical questions that may remain legitimately pending include:

- gross/cost-adjusted scalp target-quality thresholds;
- event freshness/chase distance;
- spread/slippage/drift/latency thresholds;
- scalp-specific News blackout/post-event stabilization if later evidence justifies a change;
- time-efficiency/Runner/management thresholds;
- strategy weights/correlation thresholds;
- research sample/confidence thresholds.

The following are **not** listed as open calibration merely because implementation is pending:

```text
H1/M15/M5/H4/M1 authority
SMALL/MEDIUM/NORMAL account profiles and canonical bands
8%/16% aggressive-mode semantics
disabled-by-default reset
one-fresh-reentry / three-loss cooldown baseline
1800s provider TTL baseline
PRE_CLOSE/reopen baseline
bounded analytical concurrency capability
future REAL governance path
```

## 16. Final verdict

```text
RELEASE PASS
RELEASE FAIL
RELEASE BLOCKED
SOFTWARE VERIFIED — EXTERNAL PROOF PENDING
SCALP CALIBRATION PENDING
```

Verdict identifies exact revision/scope.

## 17. Current state

```text
AUDIT RESULT: NOT RUN
Reason: production implementation and connected evidence do not yet exist; documentation freeze is still being completed.
```

This template itself is frozen; its future audit result is not pre-filled.