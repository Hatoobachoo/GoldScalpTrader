# GoldScalpTrader — Audit 2 Documents → Code → Tests/Evidence Compliance

**Status:** FROZEN AUDIT PROTOCOL — NOT RUN
**Version:** 1.0-preservation-first-doc-code-test
**Authority:** Compliance between canonical `Documents/`, actual source, deterministic tests and explicit external/scalp-calibration evidence boundaries.

## 1. Audit question

> Does the current canonical manual—including preserved reference features/defaults and genuine scalp-specific deltas—match the code that actually runs, and is each behavior proved or honestly classified?

## 2. PASS is narrow

A PASS means the synchronized revision satisfies exercised deterministic software contracts. It does not prove MT5 connectivity, broker schedule, fills/slippage/latency, future strategy edge, future REAL authorization or fresh-machine continuity.

## 3. Traceability matrix

For each topic/feature record:

```text
Document owner
Preserved / scalp-specific / operator-directed classification where inherited
Behaviour/invariant
Implementation owner
Runtime path
Focused tests
Negative/UNKNOWN tests
Persistence/recovery impact
Operator presentation
Research/evidence impact
External/scalp-calibration requirement
Verdict
```

## 4. Critical compliance paths

Audit at minimum:

- one MT5 read boundary / immutable snapshot;
- causal completed-candle intelligence;
- frozen H1/M15/M5/H4/M1 roles;
- bounded analytical concurrency + one-worker parity;
- six families / Fusion / Opportunity / Timing;
- family-aware TradePlan + gross/cost-adjusted room;
- preserved SMALL/MEDIUM/NORMAL Risk profiles/bands;
- disabled-by-default aggressive 8%/16% overlay semantics;
- disabled-by-default manual reset + preserved re-entry/cooldown;
- 1800s provider TTL + PRE_CLOSE/reopen baselines;
- News UNKNOWN/LKG-cache semantics;
- Gate/Intent/sole writer/reconciliation;
- controller/fencing;
- management/partial-management/close recovery;
- StateStore/checkpoint/local backup;
- learning/research/promotion boundaries;
- dashboard blocker/Gate/profile truth;
- future governed REAL remains gated;
- secret handling / no runtime Git operations.

## 5. Contradiction handling

Do not select the most convenient version when Documents/code/tests disagree.

Classify:

```text
DOCUMENT DEFECT
CODE DEFECT
TEST DEFECT
IMPLEMENTATION GAP
EVIDENCE GAP
SCALP CALIBRATION GAP
EXTERNAL PROOF GAP
PRESERVATION VIOLATION
```

Repair via affected-graph packet.

## 6. Preservation check

If code changes an inherited Swing feature/default, require explicit canonical authorization as genuine scalp-specific change, operator-directed change or proven reference defect correction.

Absent that authorization, the reference baseline is preserved and code is non-compliant.

## 7. Source/test inventory

Compare actual tree against `MODULE_STRUCTURE.md` and `FILE_AND_TEST_CATALOG.md`. No source/test silently owns undocumented behavior; no doc claims proof from nonexistent file/test.

## 8. Evidence classification

```text
DETERMINISTIC SOFTWARE PROOF
REPLAY/RESEARCH EVIDENCE
CONNECTED READ-ONLY PROOF
CONNECTED DEMO PROOF
LOCAL RECOVERY PROOF
SCALP CALIBRATION PENDING
EXTERNAL PROOF PENDING
FUTURE REAL RELEASE PENDING
```

## 9. Current state

```text
AUDIT RESULT: NOT RUN
Reason: production implementation has not yet been rebuilt from the final frozen manual.
```

The protocol is frozen; the audit result is not pre-filled.